## Что изменилось

## Как проверить

- [ ] `pnpm lint` и `pnpm build` проходят локально
- [ ] Если бэк (`easyworkup-api` / репозиторий `back-end-`) менял DTO — прогнал `pnpm codegen` перед этим PR, `src/api/generated` не устарел
- [ ] Обновил README, если менялось поведение
